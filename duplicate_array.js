 let array = [123]

 function duplicate(arr) {
    let newArr = arr + arr
    return newArr
 }

 duplicate(array)


//  I don't know if this is what the question is asking even? Just testing some things out. Need to clarify and update.